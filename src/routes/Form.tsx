import * as React from 'react';

const Form = () => {
    return (
        <div>
            <form>
                <label htmlFor="first">First</label>
                <input type="text" id="first"/>
            </form>
        </div>
    )
};

export default Form;